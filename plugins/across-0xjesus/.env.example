# Across Protocol Data Provider - Environment Configuration

# ============================================
# API URLs (defaults shown - no changes needed)
# ============================================

# Across API Base URL
# Used for: rates, fees, liquidity limits, asset list
BASE_URL=https://app.across.to/api

# CoinGecko API Base URL (fallback for token prices)
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3

# DefiLlama Bridge API (for volume metrics)
DEFILLAMA_BASE_URL=https://bridges.llama.fi

# ============================================
# Performance Configuration
# ============================================

# Request timeout in milliseconds (default: 60000 = 60 seconds)
TIMEOUT=60000

# Rate limit: max requests per second (default: 5)
# Increase if you have higher API limits, decrease if hitting rate limits
MAX_REQUESTS_PER_SECOND=5

# ============================================
# API Key (Optional)
# ============================================

# Across Integrator ID (optional - for tracking purposes)
# Get yours at: https://docs.across.to
# Leave empty if you don't have one - plugin works without it
API_KEY=
